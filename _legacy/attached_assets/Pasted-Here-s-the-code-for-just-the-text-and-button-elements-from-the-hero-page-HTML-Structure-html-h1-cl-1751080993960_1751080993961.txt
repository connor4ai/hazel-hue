Here's the code for just the text and button elements from the hero page:
HTML Structure:
html<h1 class="title">
    <span class="title-word">AI Color Analysis</span>
</h1>
<div class="cta-button">
    <button id="startBtn">Upload Photos</button>
</div>
CSS for the Title:
css.title {
    font-size: clamp(3rem, 8vw, 6rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
    position: relative;
    display: inline-block;
}

.title-word {
    display: inline-block;
    position: relative;
    background: linear-gradient(135deg, #5D5FEF 0%, #FF6B6B 50%, #4ECDC4 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: colorShift 8s ease infinite;
    background-size: 200% 200%;
}

@keyframes colorShift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
}
CSS for the Button:
css.cta-button {
    margin-top: 3rem;
    position: relative;
    display: inline-block;
}

.cta-button button {
    position: relative;
    padding: 1.2rem 3rem;
    background: #2A2D3A;
    color: white;
    border: none;
    border-radius: 100px;
    font-size: 1.1rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    overflow: hidden;
}

.cta-button button::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #5D5FEF, #FF6B6B, #4ECDC4);
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: -1;
}

.cta-button button:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
}

.cta-button button:hover::before {
    opacity: 1;
}
Optional: Fade-in Animation
css.title { 
    animation: fadeIn 1s ease-out forwards;
    animation-delay: 0.2s;
    opacity: 0;
}

.cta-button { 
    animation: fadeIn 1s ease-out forwards;
    animation-delay: 0.4s;
    opacity: 0;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
JavaScript for Button Click (Optional):
javascriptconst button = document.getElementById('startBtn');

button.addEventListener('click', () => {
    // Animate button
    button.style.transform = 'scale(0.95)';
    setTimeout(() => {
        button.style.transform = '';
        // Add your upload functionality here
        // For example: document.getElementById('fileInput').click();
    }, 200);
});